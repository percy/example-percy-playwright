{
  "name": "example-percy-playwright-advanced",
  "private": true,
  "description": "Advanced Percy + Playwright example. Exercises the full applicable SDK feature surface. See README.md for the matrix-row coverage.",
  "scripts": {
    "test:advanced": "percy exec -- playwright test --reporter=dot",
    "test:advanced:ci": "PERCY_TOKEN=fake_token percy exec --testing -- playwright test --reporter=dot"
  },
  "devDependencies": {
    "@percy/cli": "^1.31.13",
    "@percy/playwright": "^1.0.9",
    "@playwright/test": "1.53.2",
    "playwright": "1.53.2",
    "serve-handler": "^6.1.6",
    "todomvc-app-css": "^2.4.3"
  }
}
