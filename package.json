{
  "private": true,
  "scripts": {
    "test": "percy exec -- playwright test --reporter=dot"
  },
  "dependencies": {
    "todomvc-app-css": "^2.3.0"
  },
  "devDependencies": {
    "@percy/cli": "1.0.5",
    "@percy/playwright": "^1.0.2",
    "@playwright/test": "^1.35.1",
    "playwright": "^1.20.0",
    "serve-handler": "^6.1.3"
  }
}
